<!-- loio946b72a5ac7d4c928985f3be9abd3e9f -->

# Invoke the API and MCP Server Endpoint URL

To access a deployed API artifact or an MCP server artifact, you need to invoke their endpoint URLs using supported authentication methods.

To invoke an API or an MCP server artifact, you can obtain credentials in the following two ways:

-   **Via Developer Hub**

    When accessing APIs or MCP servers secured with an authentication policy through the Developer Hub, the system generates a client ID and client secret. Use these credentials to obtain an OAuth token, which is required to call the API or the MCP server. For more information, see [Invoke an API or an MCP Server Artifact by Obtaining Credentials via Developer Hub](invoke-an-api-or-an-mcp-server-artifact-by-obtaining-credentials-via-developer-hub-e79810f.md).

-   **Via Process Integration Runtime**

    After deploying the artifact, you can invoke the API or the MCP server endpoint using a REST client. For more information, see [Invoke an API or an MCP Server Artifact by Obtaining Credentials through Process Integration Runtime](invoke-an-api-or-an-mcp-server-artifact-by-obtaining-credentials-through-process-integrat-b63baa2.md).


